<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="300dp"
    android:layout_height="wrap_content"
    android:background="@drawable/popup_design"
    android:orientation="vertical">

    <Button
        android:id="@+id/yes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/no"
        android:layout_alignBottom="@+id/no"
        android:layout_alignParentRight="true"
        android:background="@android:color/transparent"
        android:text="YES"
        android:textColor="@color/Red"
        android:textSize="14dp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/cancelRequestText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginTop="20dp"
        android:text="Are you sure you want to delete your post?"
        android:textColor="#000000"
        android:textSize="16dp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/no"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/cancelRequestText"
        android:layout_marginTop="15dp"
        android:layout_toLeftOf="@+id/ok"
        android:background="@android:color/transparent"
        android:text="NO"
        android:textColor="@color/Storytxt"
        android:textSize="14dp"
        android:textStyle="bold"
        tools:ignore="UnknownId" />

</RelativeLayout>
